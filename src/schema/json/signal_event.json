{
  "$id": "signal_event.json",
  "type": "object",
  "required": [
    "stream_id",
    "offset",
    "timestamp",
    "signal_type",
    "spacetime",
    "provenance",
    "payload"
  ],
  "properties": {
    "stream_id": { "type": "string" },
    "offset": { "type": "integer" },
    "timestamp": { "type": "string", "format": "date-time" },
    "signal_type": { "type": "string" },
    "spacetime": {
      "type": "object",
      "required": ["region"],
      "properties": {
        "region": { "type": "string" },
        "lat_bucket": { "type": ["string", "null"] },
        "lon_bucket": { "type": ["string", "null"] }
      }
    },
    "provenance": {
      "type": "object",
      "required": ["channel", "confidence"],
      "properties": {
        "channel": {
          "type": "string",
          "enum": ["internal", "external", "partner", "court_listener", "open_states", "grants_gov", "pro_publica", "cfpb", "eeoc", "dol_whd", "osha"]
        },
        "confidence": { "type": "number" },
        "source_system": { "type": ["string", "null"] },
        "source_url": { "type": ["string", "null"] }
      }
    },
    "payload": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
