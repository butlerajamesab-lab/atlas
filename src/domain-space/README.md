# Atlas domain-space runtime

This directory is reserved for the Atlas-native deterministic domain-space registry and resolver defined by `docs/ATLAS_DOMAIN_SPACE_CONTRACT.md`.

Implementation must preserve existing geographic behavior by default and fail closed for unsupported non-geographic space definitions.
